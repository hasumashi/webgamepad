<% if (!query.onepage) { %>
<!DOCTYPE html>
<html>
	<head>
		<% include ../partials/head %>
	</head>
	
	<body>
		<% include ../partials/header %>
		
		<h1>Connect with your smartphone</h1>
<% } %>
		
		<div id="content">
			<script>
				$(document).ready(function(){
					socket.emit('get-pin');
				});
			</script>

			<div class="col-1-3">
				<img src="img/phone.png">
				<img id="qr-img" class="center">
			</div>

			<div class="col-2-3">
				<h2>Scan QR code</h2>
				<div class="hr">OR</div>
				<h2>Open in your browser:</h2>
				<span class="btn btn-white">lemaze.io/<strong id="pin-code"></strong></span>
			</div>
		</div>
		
<% if (!query.onepage) { %>
		<% include ../partials/footer %>
	</body>
</html>
<% } %>
